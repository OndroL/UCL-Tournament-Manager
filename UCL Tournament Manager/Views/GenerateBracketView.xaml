<UserControl x:Class="UCL_Tournament_Manager.Views.GenerateBracketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Bracket Generation" FontSize="20" FontWeight="Bold" Margin="10"/>
        <Button Command="{Binding GenerateBracketCommand}" Content="Generate Bracket" Margin="10"/>
        <ItemsControl ItemsSource="{Binding Matches}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding MatchId}" Margin="5"/>
                        <TextBlock Text="{Binding Team1.Name}" Margin="5"/>
                        <TextBlock Text=" vs " Margin="5"/>
                        <TextBlock Text="{Binding Team2.Name}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Command="{Binding NavigateBackCommand}" Content="Back" Margin="10"/>
    </StackPanel>
</UserControl>
